class Items {
  String title;
  String subtitle;
  String img;
  String route;

  Items(
      {required this.title,
        required this.subtitle,
        required this.route,
        required this.img});
}
